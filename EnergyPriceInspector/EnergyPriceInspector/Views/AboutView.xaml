<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:EnergyPriceInspector.ViewModels"
             x:Class="EnergyPriceInspector.Views.AboutView"
             IsBusy="{Binding IsBusy}"
             Title="{Binding Title}">
    <ContentPage.BindingContext>
        <vm:AboutViewModel />
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <RefreshView IsRefreshing="{Binding IsBusy, Mode=TwoWay}">
            <ScrollView>
                <FlexLayout Direction="Column">
                    <Button Text="Cambiar busy" Command="{Binding BusyToogleCommand}" />
                </FlexLayout>
            </ScrollView>
        </RefreshView>
    </ContentPage.Content>
</ContentPage>